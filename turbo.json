{
  "pipeline": {
    "start": {
      "dependsOn": ["^start"],
      "outputs": ["dist/**", ".next/**"]
    },
    "app:dev": {
      "dependsOn": ["^app:dev"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build:watch": {
      "dependsOn": ["^build:watch"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "bundle": {
      "dependsOn": ["build:watch"],
      "outputs": ["www/**"]
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["dist/**"]
    },
    "serve": {
      "dependsOn": ["^build:watch"],
      "outputs": ["dist/**"]
    }
  }
}
