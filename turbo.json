{
  "pipeline": {
    "start": {
      "dependsOn": ["^start"],
      "outputs": ["dist/**", ".next/**"]
    },
    "app:dev": {
      "dependsOn": ["^app:dev"],
      "outputs": ["dist/**", ".next/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "dependsOn": ["^dev"],
      "outputs": ["dist/**"]
    },
    "serve": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    }
  }
}
